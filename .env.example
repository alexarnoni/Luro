# Example environment variables for production
# Copy this to .env and fill with secure values

SECRET_KEY=change-me-to-a-strong-secret
DATABASE_URL=postgresql+asyncpg://luro:change-me@db/luro
RESEND_API_KEY=
RESEND_FROM_EMAIL=noreply@seudominio.com
DEBUG=False

# Postgres container credentials (if using the provided docker-compose.prod.yml)
POSTGRES_DB=luro
POSTGRES_USER=luro
POSTGRES_PASSWORD=change-me
# --- IA ---
LLM_PROVIDER=gemini
GEMINI_API_KEY=AIzaSyD...        # obtenha no Google AI Studio
GEMINI_MODEL=gemini-2.0-flash

# Alternativa OpenAI:
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4.1-mini

# --- App/Env ---
ENV=production
SECRET_KEY=troque-por-uma-chave-aleatoria-grande
APP_NAME=Luro
DEBUG=True
# DEBUG_HTTPX_STUB=false

# --- Banco ---
DATABASE_URL=sqlite+aiosqlite:///./luro.db

# --- E-mail (Magic Link via Resend) ---
RESEND_API_KEY=xxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL="Luro <no-reply@seu-dominio.com>"
MAGIC_LINK_EXPIRY_MINUTES=15

# --- Contenção / Segurança ---
INSIGHTS_MAX_PER_MONTH=5
RATE_LIMIT_MAX=60
RATE_LIMIT_WINDOW_SECONDS=60
ENABLE_CSRF_JSON=true
